
#include "main.h"

/**
 * print_decimal - prints a digit on the stdout
 * @value: Parameter passed
 * Return: count
 */
int print_decimal(int value)
{
	int count = o;

	if (value >= 0)
	{
		
	}
	return (count);
}
